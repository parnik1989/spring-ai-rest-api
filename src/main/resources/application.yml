server:
  port: 8080

spring:
  application:
    name: spring-ai-rest-api

  # Placeholder Spring AI configuration.
  # Replace with your AI provider settings.
  ai:
    openai:
      api-key: "${OPENAI_API_KEY:}"
      model: "gpt-4.1-mini"
